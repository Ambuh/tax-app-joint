const {ipcMain}=require("electron");

const database=require("./database");

module.exports=function () {

    return {
        handleClick: function (registerLoginWindow, user = {}) {
           
        }
    }
}